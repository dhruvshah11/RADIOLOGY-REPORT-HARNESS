__pycache__/
*.pyc
.pytest_cache/

# Competition data: download from Kaggle into data/ (not redistributed here)
data/*.csv

# Build artifacts
artifacts/fold_models*.pkl
artifacts/sweep_last.json

# Stage-2 work packets embed the competition dictations/templates (not redistributed)
artifacts/llm_packet*.json

# tuning run in progress; committed when the run completes
artifacts/tune_phase3.log
